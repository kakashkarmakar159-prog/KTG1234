# KTG INTERNAL BUILD
This build keeps the existing design, pages, data structure, APIs and Socket.IO behavior.
Local CSS and page JavaScript are embedded directly inside each HTML page (internal CSS/JS).
The original source files are also retained for maintainability.
places.json remains a separate local data file so the Edit dashboard can continue saving updates.
